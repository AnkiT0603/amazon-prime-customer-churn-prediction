# Data Notes

The checked-in CSV is a compact preprocessed sample for review. Run `python src/generate_preprocessed_data.py` to recreate the full 1,200-row synthetic dataset used by the verified model reports.

The data is synthetic and Amazon Prime-style. It is appropriate for demonstrating supervised ML, SQL analysis, and retention targeting, but it should not be described as official Amazon customer data.
